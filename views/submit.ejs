<%- include('partials/header') %>

  <main class="max-w-2xl mx-auto px-4 py-16 text-center">
    <i class="fas fa-key text-5xl text-slate-600 mb-4"></i>
    <h2 class="text-2xl font-semibold text-slate-800 mb-2">Share a Secret</h2>
    <p class="text-slate-600 mb-6">Don't keep your secrets — submit them anonymously.</p>

    <form action="/submit" method="POST" class="mx-auto max-w-xl">
      <input type="text" name="secret" placeholder="What's your secret?" class="w-full px-4 py-3 border border-gray-200 rounded-md focus:outline-none focus:ring-1 focus:ring-slate-300 text-slate-700" />
      <div class="mt-4">
        <button type="submit" class="px-6 py-2 rounded-md bg-slate-700 text-white hover:bg-slate-800 transition">Submit</button>
      </div>
    </form>

  </main>

  <%- include('partials/footer') %>
